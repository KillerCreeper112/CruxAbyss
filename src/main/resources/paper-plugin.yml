name: CruxAbyss
version: '1.0'
author: killercreepr
main: killercreepr.cruxabyss.core.CruxAbyss
api-version: '1.19'
load: STARTUP
dependencies:
  server:
    CruxCore:
      load: BEFORE
      required: true # Defaults to true
      join-classpath: true # Defaults to true
    uSurvive:
      load: BEFORE
      required: false
    ModelEngine:
      load: BEFORE
    CruxTeleport:
      load: BEFORE
    CruxQuestLine:
      load: BEFORE